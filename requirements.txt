CodingDrone==1.0.4
# 아래 두 개는 CodingDrone 의 의존성이라 자동으로 설치된다 (기록용)
# pyserial>=3.4
# numpy>=1.15.4

# 노트북으로 실습할 때만 필요
jupyter

# 21~23강 영상인식 (39~49)
# ⚠️ 4.x 로 고정한다 — 5.0 에는 CascadeClassifier 와 haarcascade xml 이 없다 (23강 얼굴 인식)
# --only-binary=:all: 를 붙여야 소스 컴파일로 새지 않는다:
#   pip install --only-binary=:all: "opencv-python<5"
opencv-python<5              # 확인한 환경: 4.10.0.84 (macOS 12 Intel / Python 3.12)
matplotlib                   # 맥 주피터에서 이미지 보기 (BGR -> RGB)

# 28강 Pygame (59~61) — OpenCV 프레임을 창에 띄우고 도형·글자를 덧그린다
pygame                       # 확인한 환경: 2.6.1 (SDL 2.28.4)

# 35~36강 추적기 (72, 73, 75) — 36강 과제는 사실상 필수.
# CSRT/KCF/MOSSE 는 contrib 에만 있다. 확인한 결과 일반 opencv-python 4.10.0 에는
# TrackerCSRT_create 도 cv2.legacy 도 없다(72 참고). 필요하면 아래처럼 **교체**한다.
# 같은 cv2 폴더에 설치되므로 두 패키지를 함께 두면 충돌한다.
#   pip uninstall -y opencv-python
#   pip install "opencv-contrib-python==4.10.0.84"
# 교체하지 않으면 72/73/75 는 TrackerMIL 로 돌아간다(동작은 한다).
#
# 36강 과제에서 SAM2 까지 쓰려면 torch 가 필요한데 **Intel Mac 은 torch 2.2.2 가 마지막**이고
# 2.2.2 는 Python 3.12 까지만 지원한다. 그래서 과제용으로 Python 3.11 venv 를 따로 만들고
# 아래 버전을 고정한다 (이 저장소의 나머지 강의용 venv 는 건드리지 않는다).
#   torch==2.2.2  torchvision==0.17.2  numpy<2  opencv-contrib-python==4.10.0.84
# SAM 없이 드래그로 대상을 고르면(USE_SAM = False) torch 없이도 75 가 그대로 돈다.

# 29강 객체 탐지 (63) — 선택. torch 가 함께 딸려 와 용량이 크다
# ultralytics
