CodingDrone==1.0.4
# 아래 두 개는 CodingDrone 의 의존성이라 자동으로 설치된다 (기록용)
# pyserial>=3.4
# numpy>=1.15.4

# 노트북으로 실습할 때만 필요
jupyter

# 21강 영상인식 (39, 40) — 둘은 같은 cv2 를 설치하므로 하나만 고를 것
opencv-python                # 확인한 환경: 5.0.0.93
# opencv-contrib-python      # contrib 기능이 필요하면 이쪽 하나만 설치
matplotlib                   # 맥 주피터에서 이미지 보기 (BGR -> RGB)
